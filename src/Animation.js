import React, { Component } from 'react';

class Animation extends Component {
  render() {
    return (
      //after click the input fades out- opacity 0?
      const duration = 300;

      const defaultStyle = {
        transition: `opacity ${duration} cubic-bezier(0,.84,1,.25) `,
        opacity: 0,
      }

      //the last li child fades in at the bottom of the screen
      //it floats to it's position in the centre at the top
      //it jostles all other li's
      //input fades in again
      //button says try again
    )
  }
}

export default Animation;